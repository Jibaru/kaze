# Kaze spike workspace

This directory holds a single serialized architecture diagram (`design.md`) in
the `kaze-adl` format. When asked to review it, use the `kaze-spike` skill.

Never modify files here. You have read-only tools by design.
